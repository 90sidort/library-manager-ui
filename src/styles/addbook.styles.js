import { makeStyles } from "@material-ui/core";

const useStylesAddBook = makeStyles(() => ({
  button: {
    marginTop: "2%",
    marginRight: "2%",
    marginBottom: "1%",
  },
  buttonGroup: {
    marginLeft: "auto",
  },
  select: {
    width: "100%",
  },
}));

export default useStylesAddBook;

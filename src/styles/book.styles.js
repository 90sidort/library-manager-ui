import { makeStyles } from "@material-ui/core";

const useStylesBook = makeStyles((theme) => ({
  root: {
    minWidth: 275,
    alignItems: "center",
    justifyContent: "center",
    display: "flex",
    marginTop: "3%",
  },
}));

export default useStylesBook;

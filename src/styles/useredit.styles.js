import { makeStyles } from "@material-ui/core";

const useStylesUserEdit = makeStyles(() => ({
  button: {
    marginTop: "2%",
    marginRight: "2%",
    marginBottom: "1%",
  },
  buttonGroup: {
    marginLeft: "auto",
  },
}));

export default useStylesUserEdit;
